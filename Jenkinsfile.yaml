pipeline:
  agent: any
  tools:
    nodejs: "node"
  stages:
    - stage: "Install Dependencies" 
      steps:
         - sh "npm install" 
    - stage: "Running npm"
      steps:
         - sh "npm config ls"
    - stage: "Test" 
      steps:
         - sh "test-nodejs-app"
         - sh "echo 'testing'"
    - stage: "deploy"
      steps: 
         - sh "test-nodejs-app"
         - sh "echo 'deploying application...'"
                                                                                                                          
